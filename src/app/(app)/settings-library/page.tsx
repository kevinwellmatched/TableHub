import { PlaceholderPage } from "@/components/placeholder-page";

export default function SettingsLibraryPage() {
  return (
    <PlaceholderPage
      title="Settings Library"
      description="Collect reusable lore, places, factions, NPCs, secrets, and world material without tying it to a single active campaign."
      activeHref="/settings-library"
    />
  );
}
