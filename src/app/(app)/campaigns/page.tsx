import { PlaceholderPage } from "@/components/placeholder-page";

export default function CampaignsPage() {
  return (
    <PlaceholderPage
      title="Campaigns"
      description="Plan active table spaces for GMs and invited players. Campaign data and membership rules will be added in later slices."
      activeHref="/campaigns"
    />
  );
}
