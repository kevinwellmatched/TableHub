import { PlaceholderPage } from "@/components/placeholder-page";

export default function AccountPage() {
  return (
    <PlaceholderPage
      title="Account"
      description="Reserve space for profile, preferences, privacy, and account controls. Auth and user settings are future frontend/backend slices."
      activeHref="/account"
    />
  );
}
