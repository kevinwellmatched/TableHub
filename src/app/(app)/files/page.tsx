import { PlaceholderPage } from "@/components/placeholder-page";

export default function FilesPage() {
  return (
    <PlaceholderPage
      title="Files"
      description="Prepare a place for maps, handouts, portraits, PDFs, and other table assets. Uploads and storage are not connected yet."
      activeHref="/files"
    />
  );
}
