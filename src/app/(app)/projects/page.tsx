import { PlaceholderPage } from "@/components/placeholder-page";

export default function ProjectsPage() {
  return (
    <PlaceholderPage
      title="Projects"
      description="Prepare GM workspaces that combine systems, compendiums, settings, files, and campaigns through linked sources and overrides."
      activeHref="/projects"
    />
  );
}
